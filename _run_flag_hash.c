#include "main.h"

/**
 * _run_flag_hash - Runs the space flag function
 * @arg: The given argument
 * @next_char: Next character
 * @wb: The buffer
 * Return: 1 or int
 */
/*int _run_flag_hash(va_list arg, char next_char, word_buffer *wb)
{
	int num_char = 0;
	int num = va_arg(arg, int);

	switch (next_char)
	{
		case 'o':
			_word_buffer_append(wb, '0');
			_recursive_print_octal(num, wb);
			break;
		case 'x':
			_word_buffer_append(wb, '0');
			_word_buffer_append(wb, 'x');
			_recursive_print_hex(num, wb);
			break;
		case 'X':
			_word_buffer_append(wb, '0');
			_word_buffer_append(wb, 'X');
			_recursive_print_hexadecimal(num, wb);
			break;
		case 'a':
		case 'A':
		case 'e':
		case 'E':
		case 'f':
		case 'F':
		case 'g':
		case 'G':
			_recursive_print_int(num, wb);
			_word_buffer_append(wb, '.');
			_word_buffer_append(wb, '0');
			break;
	}
	return (num_char);
}*/
